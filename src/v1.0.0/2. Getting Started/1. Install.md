# Install

## Dependancies

```
npm install --save @fuel/provider
```

## Usage

```js
import fuel from '@fuel/provider';
import ethers from 'ethers';
import { abi } from 'erc20';

const provider = fuel.provider(window.web3.currentProvider);
const contract = new ethers.Contract(abi, ethers.utils.hexZeroPad('0x01', 20), provider);

(async () => {

  // make a deposit
  await contract.transfer(fuel.deposit, 500000);

  // check balance
  await contract.balanceOf(await provider.getAddress());

  // make a transfer
  await contract.transfer(someAddr, 500000);

  // withdraw
  await contract.transfer(fuel.withdraw, 500000);

  /*
  transfer returns receipt format:

  FuelReceipt {
    transactionHash: '0xb903239f8543d04b5dc1ba6579132b143087c68db1b2168786408fcbce568238',
    transactionIndex: null,
    blockHash: null,
    blockNumber: null,
    from: '0xb60e8dd61c5d32be8058bb8eb970870f07233155',
    to: '0xb60e8dd61c5d32be8058bb8eb970870f07233155',
    cumulativeGasUsed: '0x33bc',
    gasUsed: '0x33bc',
    contractAddress: null,
    logs: [ Event Transfer(...) ],
    logsBloom: null,
    status: '0x1'
  }
  */
})();
```
